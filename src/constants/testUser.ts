// TODO[AUTH]: Remove this file once authentication is implemented.
// Replace all references to TEMP_TEST_EMPLOYEE_ID with the authenticated user's ID
// from the session/auth context (e.g. useAuthStore().user.employeeId).
// Search for "TODO[AUTH]" across the codebase to find every usage site.
export const TEMP_TEST_EMPLOYEE_ID = 1;
